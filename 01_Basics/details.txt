01. Scope/Block ==> {}
02. Variables ==> Let, Const, Var.
    (Const can not be changed).
03. Alert()
    (Message for User).
04. Prompt("")
    (Output)
05. Data Types ==> 
    (Number, String, boolean, bigint, null, undefined, symbol) Primitive Data Types.
    (Object, Array, Function) Non Primitive Data Types.
    Primitive data types are immutable, meaning their values cannot be changed once assigned. Non-primitive data types are mutable and can be modified.
    Null is Object Data Types.
    (Typesof "";Variables).
    DataTypes changed(
        Let stringAge = String(age);
        Let numString = Number(accountName);
        "String can not b changed in Number Data Types"
    )
04. Operator (
    1.Arthamatics Operator.
    2.Assignment Operator.
    3.Comparison Operator.
    4.Logical Operator.
    )
05. Post Inrecrement & Pre Inrecrement
     a++,++a & --a;a--
06. Stack and Heap(
    Stack ==> Primitive { Copy Variables}
    Heap ==> Non Primitive {Orginal Variables}
    )
07. String Method. // !Important
08. Number Method & Math Object.
09. Math Random 
    ( console.log(Math.floor(Math.random()*(max - min + 1))+ min); )
10. Dates In Js (Object Types).    